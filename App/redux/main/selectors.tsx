export const getImages = (state: any) =>
  state?.main?.images && state.main.images;
export const getLoading = (state: any) =>
  state.main.loading;
export const getState = (state: any) => state && state;
